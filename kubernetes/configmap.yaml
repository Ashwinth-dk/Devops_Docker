apiVersion: v1
kind: ConfigMap
metadata:
  name: organizeme-config
  namespace: organizeme
data:
  NODE_ENV: "production"
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "organizeme"
  PORT: "5000"
  CLIENT_URL: "http://localhost:3000"
  VITE_API_URL: "http://localhost:5000"
